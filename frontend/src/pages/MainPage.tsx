import Menu from "../components/Menu.tsx";
import {Outlet} from "react-router-dom";

function Layout() {
    return (
        <div>
            <h1>Witaj murarzu</h1>
        </div>
    );
}

export default Layout;